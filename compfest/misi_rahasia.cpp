#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define sec second
#define fir first

int main(){
	ll n,m;cin >> n >> m;
	ll ans=0;
	bool arr[n+1][m+1]
	while(m--){
		ll a,b;cin >> a >> b;
		if(find(v.begin))
	}
}
